apiVersion: v1
kind: Service
metadata:
  name: {{ required "service.name is required"  .Values.followupService.name }}
spec:
  selector:
    app: {{ required "Chart.Name is required"  .Chart.Name }}-followup
  ports:
    - name: {{ required "service.ports.name is required" .Values.followupService.ports.name }}
      protocol: {{ required "service.ports.protocol is required" .Values.followupService.ports.protocol }}
      port: {{ required "service.ports.port is required" .Values.followupService.ports.port }}
      targetPort: {{ required "service.ports.targetPort is required" .Values.followupService.ports.targetPort }}