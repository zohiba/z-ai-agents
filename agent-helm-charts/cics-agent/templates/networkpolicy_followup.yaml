apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.followupNetworkPolicy.name }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Chart.Name }}-followup
  policyTypes:
  - Egress
  - Ingress
  {{- with .Values.followupNetworkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.followupNetworkPolicy.egress}}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}