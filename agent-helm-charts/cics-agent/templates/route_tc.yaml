{{- /* OpenShift Route: include certs only when DEPLOYMENT_TYPE=on-prem AND termination=edge */ -}}
{{- $depType    := lower (default "on-prem" .Values.env.DEPLOYMENT_TYPE) -}}
{{- $isCloud    := eq $depType "cloud" -}}
{{- $nsRelease  := .Release.Namespace -}}
{{- $name       := required "route.name is required" .Values.TCroute.name -}}
{{- $svc        := required "route.serviceName is required" .Values.TCroute.serviceName -}}
{{- $tgtPort    := default "http" .Values.TCroute.targetPort -}}
{{- $weight     := default 100 .Values.TCroute.weight -}}
{{- $hostGen    := default "true" .Values.TCroute.annotations.hostGenerated -}}
{{- $secretName := default "wxa4z-agents-tls-secret" .Values.TCroute.tls.secretName -}}
{{- $termOnPrem := default "edge" .Values.TCroute.tls.termination -}}
{{- $globalCaSync   := dig "caTrustSync" "enabled" false .Values.global -}}
{{- $localEnable    := default false .Values.TCroute.tls.enableTlsCertMount -}}
{{- $inject         := or $globalCaSync $localEnable -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ $name }}
  annotations:
    openshift.io/host.generated: "{{ $hostGen }}"
    haproxy.router.openshift.io/timeout: "3m"
spec:
  to:
    kind: Service
    name: {{ $svc }}
    weight: {{ $weight }}
  port:
    targetPort: {{ $tgtPort }}
  tls:
    # Cloud: always edge (no certs). On-prem: use value (default edge).
    termination: {{ if $isCloud }}edge{{ else }}{{ $termOnPrem }}{{ end }}
    {{- /* Only inject certs when on-prem AND termination=edge */ -}}
    {{- if and ($inject) (eq (lower $termOnPrem) "edge") }}
    {{- $sec := lookup "v1" "Secret" $nsRelease $secretName -}}
    {{- with (and $sec (index $sec.data "tls.crt")) }}
    certificate: |-
      {{ . | b64dec | nindent 6 }}
    {{- end }}
    {{- with (and $sec (index $sec.data "tls.key")) }}
    key: |-
      {{ . | b64dec | nindent 6 }}
    {{- end }}
    {{- with (and $sec (index $sec.data "ca.crt")) }}
    caCertificate: |-
      {{ . | b64dec | nindent 6 }}
    {{- end }}
    {{- end }}
