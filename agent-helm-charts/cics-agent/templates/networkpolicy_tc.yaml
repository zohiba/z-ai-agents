apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.TCnetworkPolicy.name }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Chart.Name }}-tc
  policyTypes:
  - Egress
  - Ingress
  {{- with .Values.TCnetworkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.TCnetworkPolicy.egress}}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}